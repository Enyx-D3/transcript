Place a Vosk model zip here to enable live recording preview.

Default filename expected by the app:

`vosk-model-small-en-us-0.15.zip`

The live preview is optional. Final transcription still uses the existing
Sherpa/Whisper pipeline.
